Bootstrap: docker
From: rocker/rstudio:4.3.2
Stage: base

%post
  apt update -y
  apt install -y --no-install-recommends r-cran-seurat* r-cran-biocmanager
  for pkg in genomeinfodb genomicranges iranges rsamtools s4vectors biocgenerics
  do
    apt install -y --no-install-recommends r-bioc-$pkg
  done
  apt clean && rm -rf /var/lib/apt/lists/*    

Bootstrap: docker
From: rocker/rstudio:4.3.2
Stage: final

%files from base
  /usr/lib/R /usr/lib

%post
  R -e 'install.packages("Signac")'

%runscript
  /usr/bin/R