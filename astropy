Bootstrap: docker
From: python:3.10-slim-bookworm

%post
  pip install pip-tools
  echo "ipython" >> requirements.in
  echo "matplotlib" >> requirements.in
  echo "astropy" >> requirements.in
  pip-compile requirements.in
  pip-sync requirements.txt
  rm requirements*

%runscript
  ipython