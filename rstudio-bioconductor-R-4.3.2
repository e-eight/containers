Bootstrap: docker
From: rocker/rstudio:4.3.2

%post
  apt update -y && apt install -y zlib1g-dev && apt clean && rm -rf /var/lib/apt/lists/*
  R -e 'install.packages("BiocManager")'
  R -e 'setRepositories(ind=1:3)'
  R -e ‘BiocManager::install(c("GenomeInfoDb", "GenomicRanges", "IRanges", "Rsamtools", "S4Vectors", "BiocGenerics"))'
  R -e 'install.packages(c("Signac", "Signac", "ggpubr", "hdf5r"))'
  R -e 'BiocManager::install(c("DropletUtils", "scater", "glmGlamPoi", "SingleR"))'

%runscript
  /usr/bin/R