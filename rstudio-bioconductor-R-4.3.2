Bootstrap: docker
From: rocker/rstudio:4.3.2

%post
  apt update -y
  apt install -y --no-install-recommends r-cran-* r-bioc-*
  apt clean && rm -rf /var/lib/apt/lists/*
  R -e 'setRepositories(ind=1:3); install.packages(c("Signac", "Seurat"))'

%runscript
  /usr/bin/R